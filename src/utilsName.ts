'use strict';

// the cmd name for string reverse
export const xReverse: string = 'extension.xReverse';

// the cmd name for string to lowercase
export const xToLowerCase: string = 'extension.xToLowerCase';

// the cmd name for string to Uppercase 
export const xToUpperCase: string = 'extension.xToUpperCase';